/cmd/mcpgen/mcpgen
*.exe

# Generated by `go generate` — consumers regenerate these locally.
/example/tools/

# Working notes (design/plan/training drafts). They describe a specific
# deployment rather than this library, so they stay local.
/docs/
